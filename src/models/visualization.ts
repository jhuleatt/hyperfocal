export interface VizProperties {
  cameraMake: string;
  cameraModel: string;
  confusion: number
  focalLength: number;
  aperture: number;
  subjectDistance: number;
  hyperfocalDistance: number;
  distanceUnitDisplayName: string;
}